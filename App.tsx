import React from 'react';
import {MultiButton} from './src/multiButton';
import {SafeAreaView} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <MultiButton />
    </SafeAreaView>
  );
};

export default App;
